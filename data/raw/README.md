Raw source workbooks are stored outside the deployable project at `../data/Uncleaned` in the assignment workspace.

They are intentionally not duplicated here so the GitHub Pages visualisation loads only lightweight CSV and GeoJSON files.
